mlglab:
  children:
    apic:
      hosts:
        10.X.X.X:
          ansible_host: 10.X.x.X
          aci_username: user-id # for local user ex. apic#fallback\\user-id
          aci_password: xxxx
          aci_validate_certs: no

